# Chill task tracker web app.

- Là một to-do list
    - Task trong todo list:
        - Tiêu đề
        - Deadline
    - Khi làm xong chuyển vào tab archived.
        - Lưu lúc hoàn thành task 
        - Cho xem archived task group theo thời gian (tuần, tháng,...)
    - Chia các task thành các nhóm, mỗi nhóm cho phép tạo task trong đó
    - Có một dashboard hiển thị các task đang active (selected để làm) và backlog (sort theo deadline)

    - Stat page for all time statistic, have multiple sub tab to group by last 7, 30 day or something, make some nice graph